# إصلاحات الأولوية الحرجة — 2 سبتمبر 2026

## الحالة

التعديلات محلية فقط. لم يُطبّق تحديث قاعدة البيانات على الإنتاج، ولم يُنشر الموقع، ولم تُنشأ إعلانات حقيقية للاختبار.

## ما تغيّر

1. **التحويل بعد تسجيل الدخول:** يقبل مسارًا داخليًا فقط. تُرفض الوجهات الخارجية، والشرطات الخلفية، ومحارف التحكم، والتحايلات بالترميز أو تطبيع المسار. تبقى روابط المنتجات العربية والاستعلامات الداخلية صالحة.
2. **خصوصية واتساب:** لا يُستخدم رقم الاتصال بديلًا لرقم واتساب. يجب أن يكون إذن واتساب `true` وأن يوجد رقم مخصص له، سواء في صفحة الكمبيوتر أو شريط الهاتف. عدم وصول بيانات التفضيلات يعني إخفاء وسيلة الاتصال.
3. **سلامة النشر:** رفع جميع الصور أولًا، ثم حفظ الإعلان وروابط صوره في استدعاء PostgreSQL واحد. فشل أي إدخال يعيد المعاملة كاملة؛ لا توجد رسالة نجاح إلا بعد استلام معرّف الإعلان وعدد الصور المتوقع.

### ضوابط الحفظ

- الدالة `public.publish_web_product` تستخدم `SECURITY INVOKER` و`search_path` فارغًا؛ لا تتجاوز RLS ولا تستبدل سياسات أو triggers موجودة.
- التنفيذ للمستخدم المسجل فقط؛ هوية البائع واسمه وصورته مشتقة من ملفه في قاعدة البيانات، وليست من مدخلات المتصفح. حقول الإشراف والعدادات لا تُقبل من العميل.
- الصور بين 1 و4، والأنواع المقبولة JPEG/PNG/WebP/GIF، بحد 10 MiB للصورة بعد التجهيز. تفحص الدالة وجودها وبيانات النوع والحجم في `storage.objects`. هذا ليس فحصًا لمحتوى الملف أو برنامج مكافحة برمجيات خبيثة.
- مسار كل صورة مرتبط بمعرّف المستخدم والإعلان؛ الروابط العامة تُبنى من أصل التخزين المثبت في إعداد الدالة، لا من عنوان يرسله العميل.
- يوجد قفل فوري بالواجهة لمنع النقر المتكرر وتعديل النموذج أثناء التجهيز والنشر، وقفل معاملات لكل بائع داخل الدالة.
- تُحفظ بيانات إعادة المحاولة في `sessionStorage` قبل الطلب، مع معرّف ثابت. يُحفظ إيصال دائم في `web_product_publications` داخل نفس المعاملة، بصلاحيات قراءة/إضافة للمالك فقط ودون صلاحية تعديل أو حذف للمستخدم. إعادة الطلب بنفس المعرّف تعيد الإعلان الموجود بدل إنشاء آخر؛ تغيير محتوى نفس المحاولة يُرفض.
- إيصال النشر لا يُحذف عند حذف الإعلان، لمنع إعادة إنشائه بطلب قديم؛ لكنه يُحذف عند حذف حساب صاحبه. يحتفظ الإيصال ببيانات الإعلان ومسارات الصور لأغراض إعادة المحاولة، وليس نسخة من ملفات الصور.
- إذا انقطع الاتصال بعد الطلب، تُحتفظ بالمحاولة والصور، ويظهر زر «التحقق وإعادة المحاولة». لا نعلن الفشل النهائي أو نحذف صورًا ربما ارتبطت بإعلان محفوظ.
- الرفض الصريح في أول طلب يعيد فتح النموذج للتصحيح. بعد نتيجة سابقة غير مؤكدة، يبقى السجل محفوظًا عند استمرار الرفض، مع رابط مراجعة الإعلانات؛ قد يلزم تجديد الدخول أو تدخل الدعم.
- تنظيف الرفع أفضل جهد قبل إرسال المعاملة فقط. قد تبقى ملفات غير مرتبطة بعد رفض قاعدة البيانات أو فشل التنظيف؛ لم تُضف عملية حذف آلية خطرة.

## الاختبارات المنفذة

من مجلد `storefront_web` داخل مساحة المشروع الكاملة:

```powershell
npm run test:critical
npx tsc --noEmit
npx eslint src/lib/safe-redirect.ts src/lib/seller-contact.ts src/lib/product-publication.ts tests/critical
npm run build
```

- **65 اختبارًا**: التحويلات الخطرة/الصحيحة، مصفوفة أذونات الاتصال، انقطاع الاتصال بعد الحفظ وإعادة تحميل الصفحة، فشل الصورة الثانية أو إيصال الحفظ، عدم اكتمال التأكيد، غياب الدالة، وعزل محاولات المستخدمين، وعدم إحياء إعلان محذوف بطلب قديم.
- اختبارات SQL تعمل بمحرك PostgreSQL محلي معزول عبر PGlite وبيانات اصطناعية. استخدمت سياسات ملكية المنتجات والصور ودوال توليد slug ومزامنة الأقسام الفعلية من المستودع.
- أثبتت الاختبارات رجوع إدخال المنتج والصورة الأولى عند فشل الثانية، بما في ذلك رفض RLS. اختُبر رفض مستخدم غير مسجل، ومعرّفات الآخرين، وغياب ملف التخزين، ومدخلات غير صالحة، وتكرار المعرّف.
- نجح TypeScript وبناء الإنتاج. فحص lint للملفات الجديدة والاختبارات ناجح. يوجد دين lint سابق في صفحات الواجهة المعدلة (`any` وتأثيرات React وبعض المتغيرات غير المستخدمة)؛ لا يُدّعى نجاح lint للمشروع كله.
- فُحص المتصفح المحلي: الزائر غير المسجل يُحوّل من `/mobiles/add` إلى `/login?redirectTo=/mobiles/add` وتظهر صفحة الدخول. لم يُنفذ دخول ناجح أو نشر مصادق عليه عبر المتصفح بحساب حقيقي.

## حدود الضمان

- PGlite ليس خدمة Supabase كاملة، وطلباته المحلية متسلسلة؛ اختبار إعادة الطلب لا يُعد اختبار ضغط باتصالات PostgreSQL مستقلة. يجب اختبار ذلك في staging.
- `sessionStorage` يستمر عند تحديث نفس التبويب، وليس ضمانًا عبر إغلاقه أو متصفح/جهاز مختلف أو مسح بيانات الموقع. كل محاولة جديدة مستقلة تحصل على معرّف جديد؛ لا تمنع هذه الآلية جميع أشكال الإعلانات المتشابهة.
- فحص تكرار الموديل خلال 72 ساعة بقي كما هو في الواجهة، مع إيقاف النشر عند تعذر التحقق. ليس بديلًا عن فرض سياسة الاشتراك/الحصص مركزيًا، ولم تتغير قواعد الاشتراكات هنا.
- لم يُعدّل مسار نشر تطبيق Flutter أو لوحة الإدارة. وظيفة الحفظ الآمن تخص واجهة الويب؛ لا يُدّعى فرضها على العملاء القدامى الذين يستخدمون الإدخال المباشر.
- الاختبار لا يضمن استمرار وجود ملف تخزين بعد حذفه لاحقًا. الطلب القديم لإعلان محذوف يُرفض ويحتفظ المتصفح بسجل المحاولة للمراجعة؛ راجع الإعلانات قبل التعامل مع محاولة قديمة متأخرة.

## متطلبات قبل النشر

1. مراجعة وتطبيق **الملف المحدد فقط** `../supabase/migrations/20260902090000_atomic_web_product_publication.sql` في بيئة اختبار أولًا. تجنب دفع جميع migrations القديمة دون مراجعة سجل البيئة.
2. أصل التخزين المثبت في الدالة هو `https://xcjbjqndflhzmsxiasdb.supabase.co`. عند الاختبار على مشروع آخر، عدّل إعداد `souqphone.storage_origin` داخل الدالة إلى أصل ذلك المشروع؛ لا تستخدم روابط الإنتاج لصور الاختبار.
3. اختبار حساب بائع في staging: صورة واحدة وأربع صور، انقطاع أثناء الرفع، قطع استجابة المعاملة بعد حفظها، إعادة تحميل التبويب، تكرار الضغط، تجديد جلسة منتهية، عدم تأثير triggers الإشراف، والتحقق من `ad_number` وslug وروابط الصور الفعلية.
4. فحص الصلاحيات في البيئة: `authenticated` يحتاج صلاحياته الاعتيادية على الجداول والتسلسل وقراءة metadata التخزين. لا تُعطّل RLS لمعالجة خطأ؛ راجع السياسة المحددة.
5. تطبيق تحديث قاعدة البيانات قبل نشر واجهة الويب. إذا نُشرت الواجهة قبله، سترفض النشر برسالة واضحة بدل الرجوع إلى المسار القديم غير الآمن.
6. أُنجز تحديث التبعيات الأمني محليًا بعد هذه المراجعة: انخفضت التنبيهات من **8 (7 مرتفعة وواحد متوسط)** إلى **صفر** في `npm audit` الكامل وفحص الإنتاج. لم يُستخدم `--force`. انظر `SECURITY_UPGRADE.md` للنسخ والاختبارات؛ لا يُغني ذلك عن اختبار Supabase التجريبي أو يعني خلو الموقع من كل أنواع الثغرات.

### رجوع طارئ

الدالة إضافية؛ يمكن إبقاؤها عند التراجع عن الواجهة، ولا تحتاج حذف بيانات أو تغيير RLS. لكن الرجوع لمسار النشر القديم يعيد مشكلة النجاح الجزئي؛ عند عطل النشر يُفضّل تعطيله مؤقتًا حتى تصحيح السبب. لا تحذف صور محاولات غير مؤكدة ولا تمسح سجلاتها دون التحقق من قاعدة البيانات.
