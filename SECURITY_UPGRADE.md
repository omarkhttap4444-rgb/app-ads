# تحديث التبعيات الأمني — 2 سبتمبر 2026

التغيير محلي في `storefront_web` فقط. لم تُعدّل قاعدة بيانات الإنتاج، ولم يُنشر الموقع، وبقيت إصلاحات الأيقونة وSEO والنشر السابقة محفوظة.

## الإصدارات

| الحزمة | قبل | بعد |
| --- | --- | --- |
| Next.js وeslint-config-next | 16.2.9 | 16.3.4 |
| sharp | 0.34.5 | 0.35.4 |
| PostCSS داخل Next | 8.4.31 | 8.5.23 |
| PostCSS الخاص بأدوات التنسيق | 8.5.15 | 8.5.26 |
| Tailwind CSS وPostCSS plugin | 4.3.1 | 4.3.3 |
| brace-expansion | 1.1.15 / 5.0.6 | 1.1.18 / 5.0.9 |
| browserslist | 4.28.2 | 4.28.8 |
| js-yaml | 4.2.0 | 4.3.2 |
| nanoid | 3.3.12 | 3.3.18 |

React وReact DOM بقيا على 19.2.4. ثُبّت Next وإعداد ESLint المطابق على نسخة محددة؛ باقي الإصلاحات ضمن نطاقات التوافق الموجودة. استُخدم `npm audit fix` بعد معاينة تحديثاته، دون `--force` أو overrides تتجاوز التوافق. ملف `package-lock.json` يحتوي الإصدارات الفعلية للنشر.

اختير 16.3.4 لإزالة التنبيهات المباشرة والمتوارثة عبر Next معًا؛ النشرة الخاصة بتجاوز middleware تؤكد إصلاحها بدءًا من 16.2.11، والإصدار المختار أحدث منه. [نشرة Next الرسمية](https://github.com/vercel/next.js/security/advisories/GHSA-6gpp-xcg3-4w24)، [إصدار 16.3.4](https://github.com/vercel/next.js/releases/tag/v16.3.4).

## نتائج التحقق

- `npm audit --json`: **0** تنبيهات، مقابل 8 قبل التحديث.
- `npm audit --omit=dev --json`: **0** تنبيهات.
- `npm ci`: نجح التثبيت النظيف من ملف القفل، ونجح بناء الإنتاج والاختبارات الحرجة بعده. لم يُحذف أي ملف مصدر أو بيانات مستخدم؛ مدير الحزم أعاد إنشاء `node_modules` فقط.
- `npm run test:critical`: **65/65** ناجحة، بما فيها معاملات PostgreSQL وRLS وإعادة المحاولة وخصوصية واتساب والتحويل الداخلي.
- `npm run build`: ناجح بإصدار Next.js 16.3.4، بما فيه فحص TypeScript وتوليد 21 صفحة ثابتة.
- lint للملفات الأمنية الجديدة والاختبارات ناجح. لم تُعالج أخطاء lint السابقة في بقية المشروع ولم يُدّع نجاح فحص المشروع كله.
- أُضيف `npm run test:upgrade-smoke`: **15/15** فحص HTTP ومعالجة صور ناجح على خادم الإنتاج المحلي. يشمل الرئيسية والدخول والتسجيل وإضافة إعلان، وتحميل JS/CSS، وأيقونات PNG الست وICO، وتحويل الشعار عبر محسّن Next إلى WebP، واختبار codecs PNG/JPEG/WebP/AVIF، وmanifest وrobots والخريطة الثابتة.
- اختُبرت الواجهة في المتصفح بعد التحديث: تحوّل الزائر من إضافة إعلان إلى `/login?redirectTo=/mobiles/add` وظهرت حقول الدخول. لم تُرسل بيانات دخول ولم يُنشأ إعلان حقيقي.

لإعادة فحوص HTTP، شغّل `npm run start -- --hostname 127.0.0.1 --port 3000` ثم `npm run test:upgrade-smoke` في طرفية أخرى. تقبل هذه الفحوص خادمًا محليًا فقط.

## حدود الفحص والخطوة التالية

صفر تنبيهات يعني عدم وجود تنبيهات معروفة في قاعدة npm وقت الفحص، وليس ضمانًا للأمان المطلق. الاختبارات المحلية أجريت على Windows وNode 24؛ يجب أن تختبر بيئة المعاينة ملفات sharp/SWC الخاصة بمنصة الاستضافة أيضًا. لم تُعطّل حماية محسّن الصور من عناوين الشبكة الخاصة، وفحصه هنا استخدم شعارًا محليًا؛ لا يُعد ذلك إثباتًا لتحميل كل صور Supabase عن بُعد.

فحص `npm ls --all` ليس نظيفًا بالكامل: بعد تثبيت نظيف وتحديث سجل الحزم الاختيارية وتشغيل `npm prune` بقيت تحذيرات `extraneous` لحزم WebAssembly الاختيارية (`@img/sharp-wasm32` و`@emnapi/wasi-threads`) وتبعية `@emnapi/runtime` الناقصة لمسار WASM. لم تُضف تبعيات مصطنعة إلى التطبيق لإخفاء التحذير. مسار التشغيل الأصلي المختبر على Windows x64 يستخدم sharp 0.35.4 وlibvips 8.18.6 ونجحت معالجة الصور؛ مسار WASM لم يُختبر. يلزم التحقق في بيئة الاستضافة أيضًا، ولا تُوصف نتيجة `npm ls --all` بأنها ناجحة.

الخطوة التالية: تطبيق migration النشر المحدد في `CRITICAL_FIXES.md` على مشروع Supabase تجريبي، والتحقق بحساب اختبار من النشر والصور وانقطاع الاتصال، قبل تطبيقه على الإنتاج ثم نشر الواجهة. هذه الخطوة لم تُنفّذ ضمن تحديث الحزم.
